cluster:
  name: prod-01
  server: prod-01-k8s.endpoint.kubernetes

source:
  targetRevision: stable
  helm:
    valueFiles: values.prod-01.yaml

sealedSecrets:
  enable: true

envoyGateway:
  enable: true

monitoring:
  enable: true

