cluster:
  name: stag-01
  server: stag-01-k8s.endpoint.kubernetes

source:
  targetRevision: staging
  helm:
    valueFiles: values.stag-01.yaml

sealedSecrets:
  enable: true

ingressController:
  enable: true

monitoring:
  enable: true
