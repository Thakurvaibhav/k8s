cluster:
  type: dev-01
gateway:
  defaultHttps:
    hostname: '*.dev.mydomain.com'
    certSecretName: ingress-tls

internalGateway:
  enable: true
  defaultHttps:
    hostname: '*.dev.internal-mydomain.com'
    certSecretName: internal-ingress-tls

internalProxyConfig:
  service:
    annotations:
      networking.gke.io/load-balancer-type: "Internal"

