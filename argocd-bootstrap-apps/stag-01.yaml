kind: Application
apiVersion: argoproj.io/v1alpha1
metadata:
  name: stag-01-bootstrap-apps
  namespace: argocd
spec:
  project: operations
  source:
    path: charts/app-of-apps
    repoURL: git@github.com:Thakurvaibhav/k8s.git
    targetRevision: staging # Use staging branch for staging so that changes are promoted gradually
    helm:
      valueFiles:
        - values.stag-01.yaml
  destination:
    namespace: argocd
    server: https://kubernetes.default.svc # This is the ops cluster
  syncPolicy:
    automated:
      selfHeal: true
      allowEmpty: true
      prune: true
    syncOptions:
      - Validate=true
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true
